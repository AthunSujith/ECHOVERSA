{
    "name": "echoverse-companion",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "20.x"
    },
    "dependencies": {
        "vite": "^5.0.0"
    }
}